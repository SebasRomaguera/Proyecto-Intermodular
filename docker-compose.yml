version: '3.8'

services:
  mongodb:
    image: mongo:7.0
    container_name: easytaxi-mongodb
    restart: always
    ports:
      - "27018:27017"
    environment:
      MONGO_INITDB_DATABASE: easytaxi
    volumes:
      - mongodb_data:/data/db

volumes:
  mongodb_data:
